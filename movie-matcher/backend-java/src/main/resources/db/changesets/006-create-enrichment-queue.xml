<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="006-1" author="system">
        <comment>Create enrichment_queue table for Transactional Outbox pattern</comment>

        <createTable tableName="enrichment_queue">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="external_id" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="source" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="retry_count" type="INTEGER" defaultValue="0">
                <constraints nullable="false"/>
            </column>

            <column name="error_message" type="TEXT"/>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="processed_at" type="TIMESTAMP"/>
        </createTable>
    </changeSet>

    <changeSet id="006-2" author="system">
        <comment>Add indexes for enrichment_queue</comment>

        <!-- Index for finding PENDING items -->
        <createIndex indexName="idx_enrichment_status_created" tableName="enrichment_queue">
            <column name="status"/>
            <column name="created_at"/>
        </createIndex>

        <!-- Index for finding by external_id + source -->
        <createIndex indexName="idx_enrichment_external" tableName="enrichment_queue">
            <column name="external_id"/>
            <column name="source"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
