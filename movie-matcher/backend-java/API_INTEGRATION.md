# TMDB и OMDB API Интеграция

## ✅ Реализовано

### 1. TMDB API v3 Integration

#### REST Client
- ✅ `TmdbRestClient` - Quarkus REST клиент с поддержкой:
  - `/search/movie` - поиск по названию
  - `/movie/{id}` - детали фильма
  - `/movie/{id}/credits` - актеры и съемочная группа
  - `/discover/movie` - фильтрация по жанрам, годам, рейтингу
  - `/find/{external_id}` - поиск по IMDB ID

#### Response Models
- ✅ `TmdbMovieResponse` - полная информация о фильме
- ✅ `TmdbSearchResponse` - результаты поиска
- ✅ `TmdbSearchResult` - результат поиска
- ✅ `TmdbCreditsResponse` - актеры и съемочная группа
- ✅ `TmdbGenre`, `TmdbCountry`, `TmdbLanguage`, `TmdbCompany`
- ✅ `TmdbCastMember`, `TmdbCrewMember`

#### Mapper (MapStruct)
- ✅ `TmdbMovieMapper` - type-safe маппинг с:
  - Конвертация полного ответа → Movie entity
  - Конвертация поискового результата → Movie
  - Автоматическое построение URL для постеров
  - Маппинг жанров, стран, языков, студий
  - Маппинг credits (актеры и режиссеры)

#### Data Source Adapter
- ✅ `TmdbApiDataSource` - реализация `MovieDataSource`
  - Поиск по фильтрам с русским языком (`language=ru-RU`)
  - Discover API для рандомной подборки
  - Маппинг жанров на TMDB IDs
  - Полная поддержка всех фильтров

#### Search Handler
- ✅ `TmdbSearchHandler` - Chain of Responsibility
  - Поиск на русском языке
  - Получение полных деталей + credits
  - Автоматическое сохранение в БД

### 2. OMDB API Integration

#### Mapper (MapStruct)
- ✅ `OmdbMovieMapper` - type-safe маппинг с:
  - Обработка "N/A" значений
  - Парсинг дат, рейтингов, голосов
  - Конвертация типов (movie/series)

### 3. Chain of Responsibility Pattern

**Цепочка поиска: DB → TMDB → OMDB**

```java
DatabaseSearchHandler → TmdbSearchHandler → OmdbSearchHandler
```

**Приоритет:**
1. **Database** - самый быстрый, уже сохраненные фильмы
2. **TMDB** - приоритетный API (русский язык!)
3. **OMDB** - fallback (английский язык)

**Автоматическое обогащение БД:**
- При находке через TMDB/OMDB → сохранение в БД
- Следующий поиск уже находит в БД

### 4. MapStruct Configuration

#### pom.xml
- ✅ MapStruct 1.5.5.Final
- ✅ MapStruct Processor
- ✅ Lombok-MapStruct binding
- ✅ Annotation processor paths

**Преимущества MapStruct:**
- Type-safe маппинг (ошибки компиляции вместо runtime)
- Высокая производительность (нет рефлексии)
- Явная конфигурация через аннотации
- Поддержка кастомных методов маппинга

### 5. Configuration

#### application.properties
```properties
# TMDB API
quarkus.rest-client.tmdb-api.url=https://api.themoviedb.org
tmdb.api.key=${TMDB_API_KEY:your_key}

# OMDB API
quarkus.rest-client.omdb-api.url=https://www.omdbapi.com
omdb.api.key=${OMDB_API_KEY:a84837a9}
```

## 🎯 Ключевые особенности

### 1. Русский язык из TMDB
```java
// Все запросы к TMDB с language=ru-RU
TmdbSearchResponse response = tmdbClient.searchMovies(
    apiKey,
    query,
    "ru-RU",  // ← Русский язык!
    page,
    false,
    null
);
```

**Результат:**
- `title` - "Побег из Шоушенка" (русское название)
- `originalTitle` - "The Shawshank Redemption" (оригинал)
- `overview` - описание на русском

### 2. Discover API для рандомной подборки
```java
// TMDB Discover - мощный фильтр
tmdbClient.discoverMovies(
    apiKey,
    "ru-RU",
    "popularity.desc",  // Сортировка
    page,
    "28,12",           // Жанры (Action, Adventure)
    2000,              // Год от
    2024,              // Год до
    7.0,               // Мин. рейтинг
    100                // Мин. голосов
);
```

### 3. Маппинг жанров

**Русские названия → TMDB IDs:**
```java
"Боевик" / "Action" → 28
"Комедия" / "Comedy" → 35
"Драма" / "Drama" → 18
"Ужасы" / "Horror" → 27
"Фантастика" / "Sci-Fi" → 878
// ... и т.д.
```

### 4. Автоматическое сохранение в БД

```java
// Цепочка поиска
Optional<Movie> movie = movieSearchService.searchMovie("Интерстеллар");

// Если найдено через TMDB/OMDB:
// 1. Получаем данные на русском (TMDB)
// 2. Конвертируем через MapStruct
// 3. Сохраняем в БД
// 4. Возвращаем результат
```

### 5. Полная информация о фильме

**Что получаем из TMDB:**
- ✅ Названия (русское + оригинальное)
- ✅ Описание на русском
- ✅ Постеры (poster_path, backdrop_path)
- ✅ Рейтинг и голоса
- ✅ Жанры на русском
- ✅ Страны производства
- ✅ Языки
- ✅ Студии
- ✅ Актеры (первые 10)
- ✅ Режиссер
- ✅ Дата релиза, длительность
- ✅ Бюджет и сборы

## 📋 Как получить API ключи

### TMDB API Key
1. Зарегистрироваться на https://www.themoviedb.org/
2. Перейти в Settings → API
3. Запросить API ключ (бесплатно)
4. Добавить в переменную окружения `TMDB_API_KEY`

### OMDB API Key
1. Зарегистрироваться на https://www.omdbapi.com/apikey.aspx
2. Выбрать FREE tier (1000 запросов/день)
3. Подтвердить email
4. Добавить в переменную окружения `OMDB_API_KEY`

## 🔧 Использование

### 1. Поиск фильма
```java
@Inject
MovieSearchService movieSearchService;

// Поиск по цепочке: DB → TMDB → OMDB
Optional<Movie> movie = movieSearchService.searchMovie("Побег из Шоушенка");

// Результат на русском языке!
if (movie.isPresent()) {
    System.out.println(movie.get().title); // "Побег из Шоушенка"
    System.out.println(movie.get().plot);  // Описание на русском
}
```

### 2. Подбор фильмов по фильтрам
```java
@Inject
TmdbApiDataSource tmdbDataSource;

MovieFilters filters = MovieFilters.builder()
    .genre("Боевик")
    .yearFrom(2010)
    .yearTo(2024)
    .minRating(BigDecimal.valueOf(7.0))
    .type("movie")
    .build();

// Получаем фильмы на русском
List<Movie> movies = tmdbDataSource.findByFilters(filters, 1, 20);
```

### 3. Обогащение БД
```java
@Inject
EnrichmentService enrichmentService;

// Добавить фильмы в очередь обогащения
List<String> imdbIds = List.of("tt0111161", "tt0068646", "tt0468569");
enrichmentService.enqueueMoviesForEnrichment(imdbIds, "TMDB");

// Асинхронная обработка каждые 10 секунд
// → Загрузка из TMDB → Сохранение в БД
```

## 🚀 Преимущества реализации

### 1. Паттерны ООП
- ✅ **Adapter Pattern** - унификация работы с API
- ✅ **Chain of Responsibility** - гибкая цепочка поиска
- ✅ **MapStruct** - type-safe маппинг без рефлексии

### 2. Русский язык
- ✅ TMDB возвращает все на русском (`language=ru-RU`)
- ✅ Названия, описания, жанры на русском
- ✅ OMDB как fallback (английский)

### 3. Автоматическое обогащение
- ✅ Найденные фильмы сохраняются в БД
- ✅ Следующий поиск уже из БД (быстрее)
- ✅ Уникальность по IMDB ID

### 4. Производительность
- ✅ MapStruct - код генерируется при компиляции
- ✅ Нет рефлексии в runtime
- ✅ Type-safe маппинг

### 5. Расширяемость
- ✅ Легко добавить новые источники данных
- ✅ Легко добавить новые обработчики в цепочку
- ✅ Легко добавить новые поля в маппинг

## 📊 Диаграмма работы

```
User Request
    ↓
MovieSearchService
    ↓
┌─────────────────────────────────────┐
│  Chain of Responsibility            │
│                                     │
│  DatabaseSearchHandler              │
│         ↓ (не найдено)              │
│  TmdbSearchHandler                  │
│    - Поиск на русском               │
│    - MapStruct маппинг              │
│    - Сохранение в БД ✓              │
│         ↓ (не найдено)              │
│  OmdbSearchHandler                  │
│    - Поиск на английском            │
│    - MapStruct маппинг              │
│    - Сохранение в БД ✓              │
└─────────────────────────────────────┘
    ↓
Movie entity (на русском!)
```

## 🎬 Пример данных

### TMDB Response (русский язык)
```json
{
  "title": "Побег из Шоушенка",
  "original_title": "The Shawshank Redemption",
  "overview": "Бухгалтер Энди Дюфрейн обвинён в убийстве...",
  "genres": [
    {"id": 18, "name": "Драма"},
    {"id": 80, "name": "Криминал"}
  ],
  "vote_average": 8.7,
  "release_date": "1994-09-23"
}
```

### Movie Entity в БД
```java
Movie {
  title = "Побег из Шоушенка"      // Русский!
  originalTitle = "The Shawshank Redemption"
  plot = "Бухгалтер Энди..."        // Русский!
  imdbRating = 8.7
  genres = [Drama, Crime]           // Русские названия
}
```

## 🔜 Следующие шаги

1. ✅ TMDB API интеграция
2. ✅ OMDB API интеграция  
3. ✅ MapStruct mappers
4. ✅ Chain of Responsibility
5. ⏳ Application Services (RoomApplicationService)
6. ⏳ Liquibase миграции для русского языка
7. ⏳ WebSocket обновления
8. ⏳ Flutter приложение
